  <%Reporter.find(params[:id].to_i).articles.distinct.each do |article|%>
    <div class="col-sm-4">
      <%= render partial: "./articles/article_display", locals: { article: article } %>
    </div>
  <%end%>
